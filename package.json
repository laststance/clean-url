{
  "name": "clean-url",
  "version": "1.0.1",
  "description": "Removes tracking parameters from url",
  "license": "ISC",
  "author": "Ryota Murakami <dojce1048@gmail.com> (https://github.com/laststance)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:unit": "jest tests/unit",
    "test:e2e": "playwright test",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "install:playwright": "playwright install",
    "lint": "eslint *.js tests/",
    "build": "npm run test:unit && rm -f clean-url-extension.zip && npx bestzip clean-url-extension.zip manifest.json popup.html popup.js popup.css background.js clean-url-logic.js icons/ privacy-policy.md && echo '✅ Chrome Extension ZIP created: clean-url-extension.zip'",
    "build:zip": "rm -f clean-url-extension.zip && npx bestzip clean-url-extension.zip manifest.json popup.html popup.js popup.css background.js clean-url-logic.js icons/ privacy-policy.md",
    "zip:check": "unzip -l clean-url-extension.zip",
    "validate": "npm run lint && npm run test:unit"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/chrome": "^0.0.258",
    "bestzip": "^2.2.1",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "keywords": [
    "chrome-extension",
    "privacy",
    "url-cleaning",
    "tracking-parameters",
    "utm-removal"
  ]
}
